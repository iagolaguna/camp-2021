const log = (message) => console.log(message)
const error = (message) => console.error(message)
const info = (message) => console.info(message)

export const exports = { log, error, info }
export default exports;
